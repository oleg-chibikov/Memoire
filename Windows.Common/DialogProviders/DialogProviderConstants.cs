namespace Mémoire.Windows.Common.DialogProviders
{
    static class DialogProviderConstants
    {
        public const string JsonFilesFilter = "Json files (*.json)|*.json;";
        public const string DefaultFilePattern = "Mémoire.json";
    }
}
