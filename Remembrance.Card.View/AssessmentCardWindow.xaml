<view1:AnimatedWindow
    x:Class="Remembrance.Card.View.AssessmentCardWindow"
    x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    MaxWidth="1000"
    MaxHeight="800"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:Remembrance.Card.View"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:contracts="clr-namespace:Remembrance.Card.ViewModel.Contracts;assembly=Remembrance.Card.ViewModel.Contracts"
    xmlns:view1="clr-namespace:Scar.Common.WPF.View;assembly=Scar.Common.WPF.View.AnimatedWindow"
    lex:LocalizeDictionary.DesignCulture="en"
    lex:LocalizeDictionary.OutputMissingKeys="True"
    lex:ResxLocalizationProvider.DefaultAssembly="Remembrance.Resources"
    lex:ResxLocalizationProvider.DefaultDictionary="Texts"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance contracts:IAssessmentCardViewModel}"
    SizeToContent="WidthAndHeight"
    Icon="IconAlt.ico">
    <view1:AnimatedWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="/Remembrance.Resources;component/XamlResources/Styles.xaml" />
                <ResourceDictionary
                    Source="/Remembrance.Resources;component/XamlResources/Converters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </view1:AnimatedWindow.Resources>
    <Grid
        Margin="{StaticResource WindowGridMargin}">
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto" />
            <RowDefinition
                Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel
            Orientation="Horizontal"
            Grid.Row="0">
            <view:WordControl
                FontSize="{StaticResource HugeFontSize}"
                FontWeight="Bold"
                DataContext="{Binding Word}" />
            <TextBlock
                FontSize="{StaticResource MediumFontSize}"
                Margin="{StaticResource CommonLeftMargin}"
                Text="{Binding LanguagePair, StringFormat='({0})'}" />
            <TextBlock
                FontSize="{StaticResource HugeFontSize}"
                Margin="{StaticResource CommonLeftMargin}"
                Text="-"
                Visibility="{Binding Accepted, Converter={StaticResource NotNullToVisibilityConverter}}" />
            <TextBlock
                FontSize="{StaticResource HugeFontSize}"
                Margin="{StaticResource CommonLeftMargin}"
                Text="{Binding Path=CorrectAnswer}"
                Visibility="{Binding Accepted, Converter={StaticResource NotNullToVisibilityConverter}}">
                <TextBlock.Style>
                    <Style
                        TargetType="{x:Type TextBlock}">
                        <Style.Triggers>
                            <DataTrigger
                                Binding="{Binding Accepted}"
                                Value="False">
                                <Setter
                                    Property="Foreground"
                                    Value="Red" />
                            </DataTrigger>
                            <DataTrigger
                                Binding="{Binding Accepted}"
                                Value="True">
                                <Setter
                                    Property="Foreground"
                                    Value="ForestGreen" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
        <DockPanel
            Visibility="{Binding Accepted, Converter={StaticResource NullToVisibilityConverter}}"
            Margin="{StaticResource CommonTopMargin}"
            Grid.Row="1">
            <view:AnswerControl
                x:Name="AnswerControl"
                DockPanel.Dock="Right"
                HorizontalAlignment="Right"
                FontSize="{StaticResource MediumFontSize}" />
        </DockPanel>
        <StackPanel
            TextBlock.FontSize="{StaticResource MediumFontSize}"
            Visibility="{Binding Accepted, Converter={StaticResource NotNullToVisibilityConverter}}"
            Margin="{StaticResource CommonTopMargin}"
            Grid.Row="1">
            <TextBlock
                Foreground="Red"
                Text="{lex:Loc WrongAnswer}"
                Visibility="{Binding Accepted, Converter={StaticResource ReverseBoolToVisibilityConverter}}" />
            <TextBlock
                Foreground="ForestGreen"
                Text="{lex:Loc CorrectAnswer}"
                Visibility="{Binding Accepted, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</view1:AnimatedWindow>